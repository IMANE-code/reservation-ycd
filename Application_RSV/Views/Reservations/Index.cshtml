@model IEnumerable<Application_RSV.Models.Reservation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.accepter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.accepter)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idReservtion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idReservtion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idReservtion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
